[package]
name = "bloat_lib"
version = "0.1.0"
edition = "2024"
description = "Shared compile-time bloat — compiled once by cargo's dependency caching"

[lib]
crate-type = ["rlib"]

[dependencies]
