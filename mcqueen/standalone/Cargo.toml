[package]
name = "standalone"
version = "0.1.0"
edition = "2024"
description = "Standalone executable — window, watcher, hot-reload loop"

[dependencies]
mini_engine = { path = "../engine" }
macroquad = "0.4"
notify = { version = "6", default-features = false, features = [
    "macos_kqueue",
] }
