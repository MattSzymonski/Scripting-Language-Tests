[package]
name = "flappy"
version = "0.1.0"
edition = "2021"
description = "A small Flappy Bird game built on mini_engine"

[features]
default = []
cs = ["dep:libloading"]

[dependencies]
mini_engine = { path = "../engine" }
game_rs = { path = "../game_rs" }
macroquad = "0.4"
libloading = { version = "0.8", optional = true }
