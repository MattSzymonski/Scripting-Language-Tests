[package]
name = "patch_plugin"
version.workspace = true
edition.workspace = true
description = "Patch cdylib — contains replacement implementations loaded alongside the original plugin.dll"

[lib]
crate-type = ["cdylib"]

[dependencies]
