[package]
name = "plugin"
version.workspace = true
edition.workspace = true
description = "cdylib plugin demonstrating DLL live-patching — exports a sentinel and patchable functions"

[lib]
crate-type = ["cdylib"]

[dependencies]
