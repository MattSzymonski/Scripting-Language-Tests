[package]
name = "heavy_common"
version = "0.1.0"
edition = "2021"
description = "Shared dependency for heavy plugins — simulates an engine core module with real dependencies"

[lib]
crate-type = ["rlib"]

[dependencies]
image = "0.24"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
