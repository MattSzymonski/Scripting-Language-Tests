[package]
name = "standalone"
version = "0.1.0"
edition = "2021"
description = "Mini-engine host: window + engine, watches the project crate and hot-swaps it"

[dependencies]
mini_engine = { path = "../engine" }
macroquad = "0.4"
libloading = "0.8"
notify = "6"
